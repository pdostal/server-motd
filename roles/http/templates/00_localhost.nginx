server {
  listen 80;
  server_name 127.0.0.1 {{ ansible_hostname }}.pdostal.cz;

  root /var/www;
  index index.html;

  location / {
    try_files $uri $uri/ =404;
  }

  location ~ /\.ht {
    deny all;
  }
}
